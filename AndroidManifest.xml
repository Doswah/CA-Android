<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.android"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="ca.android.Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ca.android.Dashboard"
            android:label="@string/title_activity_login" />
        <activity
            android:name="ca.android.MainController"
            android:label="@string/app_name" />
        <activity
            android:name="ca.android.OneActivity"
            android:label="@string/title_activity_one" />
        <activity
            android:name="ca.android.TwoActivity"
            android:label="@string/title_activity_two" />
        <activity
            android:name="ca.android.ThreeActivity"
            android:label="@string/title_activity_three" >
        </activity>
    </application>

</manifest>
